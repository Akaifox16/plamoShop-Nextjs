import { useState } from "react"

export default function StockInList(){
    const [stockIn,setStockIn] = useState([])

    return (
        <div>
            <p>this is where to display stock-in records of that product</p>
        </div>
    )
}